import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import { Nterior } from "./components/Nterior";
import reportWebVitals from './reportWebVitals';

ReactDOM.render(
  <React.StrictMode>
    <Nterior />
  </React.StrictMode>,
  document.getElementById('root')
);

reportWebVitals();
